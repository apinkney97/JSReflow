%!
/Times-Roman findfont 6 scalefont setfont
/y 750 def
/x 50 def
/n 0 def
/len 0 def
(font*) {
	/n n 1 add def
	dup x y moveto n 6 string cvs show (: ) show show (: ) show
	status pop pop pop dup
	20 string cvs show
	/len exch len add def
	pop
	/y y 7 sub def
	y 50 le {/y 750 def /x x 200 add def} if
	x 450 gt {showpage /x 50 def} if
} 100 string filenameforall
x y moveto
(Total length: ) show
len 20 string cvs show
showpage
